---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.interaction.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.services.interaction.name }}
spec:
  selector:
    app: {{ .Values.services.interaction.name }}
  ports:
    - port: {{ .Values.services.interaction.port }}
      targetPort: {{ .Values.services.interaction.port }}
      name: http
    - port: {{ .Values.services.interaction.grpcPort }}
      targetPort: {{ .Values.services.interaction.grpcPort }}
      name: grpc
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.interaction.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.services.interaction.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.interaction.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.interaction.name }}
    spec:
      containers:
        - name: {{ .Values.services.interaction.name }}
          image: "{{ .Values.services.interaction.image.repository }}:{{ .Values.services.interaction.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.interaction.port }}
              name: http
            - containerPort: {{ .Values.services.interaction.grpcPort }}
              name: grpc
          envFrom:
            - configMapRef:
                name: tiktok-db-config
            - secretRef:
                name: tiktok-db-secrets
          env:
            - name: PORT
              value: {{ .Values.services.interaction.port | quote }}
            - name: INTERACTION_HTTP_PORT
              value: {{ .Values.services.interaction.port | quote }}
            - name: INTERACTION_GRPC_PORT
              value: {{ .Values.services.interaction.grpcPort | quote }}
            - name: INTERACTION_DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: tiktok-db-config
                  key: DB_HOST
            - name: INTERACTION_DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: tiktok-db-config
                  key: DB_PORT
            - name: INTERACTION_DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: tiktok-db-config
                  key: DB_NAME
            - name: INTERACTION_DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: tiktok-db-config
                  key: DB_USER
            - name: GRPC_INTERACTION_URL
              value: "0.0.0.0:{{ .Values.services.interaction.grpcPort }}"
          resources:
{{ toYaml .Values.services.interaction.resources | indent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.interaction.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.interaction.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

{{- if .Values.services.interaction.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.services.interaction.name }}-hpa
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.services.interaction.name }}
  minReplicas: {{ .Values.services.interaction.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.interaction.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.interaction.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
